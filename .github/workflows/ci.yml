name: CI

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  validate:
    name: Validate workspace
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rust-src
          targets: riscv32imac-unknown-none-elf

      - name: Install nightly docs toolchain
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rust-src
          targets: riscv32imac-unknown-none-elf

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Metadata
        run: cargo metadata --no-deps --format-version 1

      - name: Format
        run: cargo fmt --check --all

      - name: Clippy
        run: cargo clippy --workspace --target riscv32imac-unknown-none-elf --all-targets -- -D warnings

      - name: AI slop check
        run: cargo xtask lint

      - name: Check
        run: cargo check --workspace --target riscv32imac-unknown-none-elf

      - name: Check all features
        run: cargo check --workspace --target riscv32imac-unknown-none-elf --all-features

      - name: Host tests
        shell: bash
        run: |
          HOST_TARGET="$(rustc -vV | sed -n 's/^host: //p')"
          RUSTFLAGS="--cfg nesso_host_tests" \
            cargo test --manifest-path tests/host/Cargo.toml --target "${HOST_TARGET}"

      - name: Host clippy
        shell: bash
        run: |
          HOST_TARGET="$(rustc -vV | sed -n 's/^host: //p')"
          RUSTFLAGS="--cfg nesso_host_tests" \
            cargo clippy --manifest-path tests/host/Cargo.toml \
              --target "${HOST_TARGET}" --all-targets -- -D warnings

      - name: Build
        run: cargo build --workspace --target riscv32imac-unknown-none-elf

      - name: Release build
        run: cargo build --workspace --target riscv32imac-unknown-none-elf --release

      - name: Docs
        env:
          RUSTDOCFLAGS: -D warnings
        run: |
          cargo +nightly rustdoc -p nesso --lib \
            -Zbuild-std=core,alloc \
            --target riscv32imac-unknown-none-elf \
            --all-features \
            -- -D warnings

      - name: Publish dry-run
        run: cargo publish -p nesso --dry-run --allow-dirty
